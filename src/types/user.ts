type IUser = {
  name: string;
  email: object;
  password: object;
  emailVerified: boolean;
  emailVerificationToken: string;
  resetToken: string;
  token: object;
  _id: object;
};

export type { IUser };
